import styled from "styled-components";

const MainWorks = () => {
    return(
        <MainWorksWrapper>
        </MainWorksWrapper>
    );
};

const MainWorksWrapper = styled.div`
	height: 100vh;

`;

export default MainWorks;